package features

import org.junit.jupiter.api.Nested
import org.junit.jupiter.api.Test

internal class DefinitionTest {

    @Nested
    inner class GoToDefinition {
        @Test
        fun `should go to definition when definition action with in the same file`() {


            // load files
            // build request
            // assert result
        }

        @Test
        fun `should goto definition when definition action with in different file`() {
        }
    }
}
